# Configuration for Network Scanner

# The default network to scan if none is provided on the command line.
DEFAULT_TARGET="127.0.0.1"

# Directory where reports will be saved.
REPORT_DIR="Reports"

# Default Nmap flags for a quick scan.
NMAP_FLAGS="-T4 -F"
